---
description: Run a health check over the Obsidian vault at ~/dev/knowledge
---

Use the `vault-lint` skill to run a health check on the vault.

Default mode is report-only. Do not edit any files unless the user explicitly asks for fix mode.

If $ARGUMENTS is "fix", enter fix mode and confirm each edit individually.
