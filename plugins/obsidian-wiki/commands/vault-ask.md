---
description: Query the Obsidian wiki at ~/dev/knowledge with citations
---

Use the `vault-query` skill to answer this question against the vault: $ARGUMENTS

Read `~/dev/knowledge/Home.md` first as the index. Cite every claim with `[[Page Name]]`.
If the answer has lasting value, ask whether to file it back as a new wiki page.
