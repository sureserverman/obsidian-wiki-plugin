---
description: Ingest a source into the Obsidian wiki at ~/dev/knowledge
---

Use the `vault-ingest` skill to process this source: $ARGUMENTS

If $ARGUMENTS is empty, list files under `~/dev/knowledge/raw/` and ask which one to ingest.
