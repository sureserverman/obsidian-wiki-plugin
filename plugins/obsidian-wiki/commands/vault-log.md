---
description: Show recent activity from the Obsidian vault log at ~/dev/knowledge/log.md
---

Read `~/dev/knowledge/log.md` and show the last 10 entries (lines starting with `## [`).

Group them by type (ingest / query / lint / schema) and summarize the recent activity in
2–3 sentences. If $ARGUMENTS is a number, show that many entries instead of 10.
