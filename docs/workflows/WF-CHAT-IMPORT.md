# WF-CHAT-IMPORT — preview-only conversation intake

1. Validate the archive and supported record shape.
2. Create a sanitized immutable revision and protected locator mapping.
3. Inventory every event and attachment before topic extraction.
4. Produce a coverage ledger and a batch preview.

Inputs are inert data. This workflow ends before general-vault publication.
